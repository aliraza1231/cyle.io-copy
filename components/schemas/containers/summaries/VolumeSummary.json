{
  "title": "VolumeSummary",
  "type": "object",
  "description": "A summary of relevant volume information.",
  "required": [
    "id",
    "hash",
    "config"
  ],
  "properties": {
    "id": {
      "$ref": "../../ID.json"
    },
    "hash": {
      "type": "string",
      "description": "A unique hash for the given volume."
    },
    "config": {
      "$ref": "../config/ContainerVolume.json"
    }
  }
}
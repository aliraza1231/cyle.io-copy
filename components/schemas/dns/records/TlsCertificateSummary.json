{
  "title": "TLSCertificateSummary",
  "type": "object",
  "description": "A TLS record certificate",
  "required": [
    "id",
    "generated",
    "wildcard_child"
  ],
  "properties": {
    "id": {
      "$ref": "../../ID.json"
    },
    "generated": {
      "$ref": "../../DateTime.json"
    },
    "wildcard_child": {
      "type": "boolean",
      "description": "A value where true represents that the certificate is using a shared wildcard cert."
    }
  }
}